# Server Configuration
PORT=3000  # Optional: Change this to run on a different port (e.g., 3005)

# Database (Neon)
DATABASE_URL=postgres://neon:npg@localhost:5432/neondb?sslmode=no-verify

# Optional: Database Connection Pool Settings (Vercel optimized defaults are used if not set)
# DB_POOL_MAX=1              # Max connections per serverless instance (default: 1 on Vercel, 10 locally)
# DB_IDLE_TIMEOUT=10         # Seconds before idle connection closes (default: 10 on Vercel, 20 locally)
# DB_CONNECT_TIMEOUT=5       # Seconds to wait for connection (default: 5)

# Email (Resend)
RESEND_API_KEY=...
EMAIL_FROM=noreply@luzimarket.shop

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=...
STRIPE_SECRET_KEY=...
STRIPE_WEBHOOK_SECRET=...

# NextAuth
NEXTAUTH_URL=http://localhost:3000  # Update port if using custom PORT
NEXTAUTH_SECRET=...

# Optional: AI Image Generation
OPENAI_SECRET_KEY=sk-...  # For product/category image generation

# Optional: Vercel Blob Storage (production only)
BLOB_READ_WRITE_TOKEN=...  # Falls back to local storage in dev